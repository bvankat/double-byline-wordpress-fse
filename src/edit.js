import { __ } from '@wordpress/i18n';

export default function Edit() {
    return (
        <p>{__('Double Byline block (dynamic, frontend-rendered)', 'hanscomparkstudio-double-byline')}</p>
    );
}
